apiVersion: v1
kind: Service
metadata:
  name: notificationservice-nodeport
  namespace: orderflow
spec:
  selector:
    app: notificationservice
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30082
  type: NodePort