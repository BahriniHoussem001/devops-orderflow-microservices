apiVersion: v1
kind: Service
metadata:
  name: orderservice-nodeport
  namespace: orderflow
spec:
  selector:
    app: orderservice
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30081
  type: NodePort